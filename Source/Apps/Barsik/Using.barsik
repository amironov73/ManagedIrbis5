#u System.IO

println ("Before");

using (m = new MemoryStream())
{
    m.WriteByte (to_byte (31));
    m.WriteByte (to_byte (32));
    m.WriteByte (to_byte (33));

    println (m.Position);
}

println ("After");
