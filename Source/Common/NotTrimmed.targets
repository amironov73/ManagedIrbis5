<Project>

    <!-- Compile to NON-native image -->
    <PropertyGroup>
        <RestoreLockedMode>true</RestoreLockedMode>
        <PublishTrimmed>false</PublishTrimmed>
        <PublishSingleFile>true</PublishSingleFile>
        <StripSymbols>true</StripSymbols>
    </PropertyGroup>

</Project>
