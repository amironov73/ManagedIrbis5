func cmp (first, second)
{
    r = first <=> second
    println (first, " <=> ", second, " \t==> ", r)
}

cmp (0, 0)
cmp (0, 2)
cmp (3, 0)

println()

cmp (0.0, 0.0)
cmp (0.0, 2.0)
cmp (3.0, 0.0)

println()

cmp ("fi", "fi")
cmp ("fi", "se")
cmp ("th", "fi")
